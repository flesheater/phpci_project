setup:
    shell:
      - "cd /var/www/html/phpci_project && git pull origin master"
      - "cd /var/www/html/phpci_project && composer install"
      - "cd /var/www/html/phpci_project && ./vendor/bin/robo setup"
